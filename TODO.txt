FOCUS: App — get it ready for users

RELEASE ORDER
1. App (now) — logbook polish, offline
2. Digifi web — Pages, QR companion, mock credits for testers
   DIGIFI GRID EDITING (pain from real use)
   - Problem: right page skipped a row / merged rows; fixing misaligned cells is hard without spreadsheet UX.
   - Need: multi-cell / multi-row select, copy-paste, cut, and drag-drop (like Excel / Google Sheets).
   - Cut / drag must leave the source empty (move, not duplicate).
   - Goal workflow: select the correct block of rows/columns, then paste or drag into the correct position to repair alignment.

────────────────────────────────────────
APP NOW
────────────────────────────────────────

1. OFFLINE LOGBOOK — cache gap still open
   - Signed-in users load from IndexedDB first, then sync from Supabase when online.
   - Full logbook is available offline ONLY if it was previously synced to IndexedDB on that device.
   - First visit with empty cache + no internet = empty logbook (gap to fix).
   - Still open: cold-start empty cache; verify on device at altitude / after landing.

2. DEVICE / SIGNING SMOKE (when ready) — void survives refresh; amend; guest QR; main CFI; paper + e-sign endorsements

3. FLIGHT SIGNATURES FETCH — URL too long on large logbooks (live dashboard)
   - refreshFlightSignatures / fetchSignaturesForEntries sends all entry IDs in one PostgREST .in() GET.
   - Huge URL → net::ERR_FAILED (often looks like CORS). Crew profiles still load; signature badges may miss.
   - Fix: batch .in() queries (e.g. 50–100 IDs) or server RPC / join so IDs aren’t all in the query string.
   - Touch: useFlightSigning.ts, dashboard.vue (refreshFlightSignatures); check AuditTrail paths too.

4. AUTOFI ON iOS — after `dev` is on `main`
   - TestFlight talks to production (`www.logifi.io`), not the `dev` preview. Wait for the merge.
   - Production: `/api/flica/*` live, `FLICA_CREDENTIALS_KEY` set, `flica_integrations` table exists.
   - Then: `NUXT_PUBLIC_API_BASE=https://www.logifi.io`, `npm run cap:sync`, new TestFlight.
   - Smoke: Connect FLICA (no white screen), fetch today’s pairing (AC-swap legs + PIC from end crew).

5. iOS LOGBOOK CARDS — rework
   - Derek has a new card design in mind. Don’t invent a layout first — sketch with him, then implement.
   - Scope: entry cards on the iOS dashboard/list (spacing, hierarchy, what shows at a glance). Match existing iOS shells, not a new SaaS look.

────────────────────────────────────────
LATER (after app is user-ready)
────────────────────────────────────────
- Retire instructor (soft end vs Remove) + optional Main-switch grace
- Digifi/OCR for paper endorsements; endorsement PDF export
- Manual and airline imports in the same day

────────────────────────────────────────
BRANCHES
────────────────────────────────────────
- dev — app work, Digifi web testing
